<%= form_with(model: event_document, local: true) do |form| %>
  <% if event_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_document.errors.count, "error") %> prohibited this event_document from being saved:</h2>

      <ul>
      <% event_document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.number_field :event_id, id: :event_document_event_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :event_document_user_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :event_document_name %>
  </div>

  <div class="field">
    <%= form.label :file %>
    <%= form.text_field :file, id: :event_document_file %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
