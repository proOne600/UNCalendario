<%= form_with(model: event_tag, local: true) do |form| %>
  <% if event_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_tag.errors.count, "error") %> prohibited this event_tag from being saved:</h2>

      <ul>
      <% event_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.number_field :event_id, id: :event_tag_event_id %>
  </div>

  <div class="field">
    <%= form.label :id_category %>
    <%= form.number_field :id_category, id: :event_tag_id_category %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :event_tag_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :event_tag_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
